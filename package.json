{
  "name": "webhook-inbox-mcp-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8791",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.81.0"
  }
}
